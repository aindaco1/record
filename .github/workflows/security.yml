name: CodeQL

on:
  push:
    branches: [main]
  schedule:
    - cron: "23 9 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  changes:
    name: CodeQL change scope
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      docs_only: ${{ steps.scope.outputs.docs_only }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Classify complete change range
        id: scope
        run: python3 scripts/ci/change-scope.py --github-event --github-output

  analyze-swift:
    name: Analyze Swift
    needs: changes
    if: needs.changes.outputs.docs_only != 'true'
    # A skipped documentation push must not cancel an active source scan.
    concurrency:
      group: codeql-${{ github.ref }}
      cancel-in-progress: true
    runs-on: xcode-27
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Select pinned released Xcode
        run: ./scripts/ci/select-stable-xcode.sh

      - name: Resolve locked dependencies
        run: |
          swift package resolve
          git diff --exit-code -- Package.resolved

      # github/codeql-action v4.38.1, pinned to a verified commit.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@1c5b675653bb5c22dbe9b12b556ec555138e09fd
        with:
          languages: swift
          build-mode: manual

      - name: Build
        run: >-
          swift build -c release --arch arm64
          --disable-automatic-resolution

      - name: Analyze
        uses: github/codeql-action/analyze@1c5b675653bb5c22dbe9b12b556ec555138e09fd
        with:
          category: /language:swift
