# Transcript cleanup evaluation — September 23, 2026

This is testing evidence, not a product fix or a release. The adapter exercised
the unchanged Record echo suppressor, refinement coordinator and on-device
Foundation Models adviser on an Apple M1 Max running macOS 27.0 with Xcode
27.0 (`27A266a`). No recording, microphone, screen capture, ASR or MacWhisper was
invoked. Only the reviewed hand-authored synthetic corpus reached hosted Jev.

## Results

The explicit request preview completed with zero remote requests. An earlier
preview stopped before authentication when the shared Platform checkout's HEAD
advanced; a separate detached checkout now holds the reviewed pin.

The live run used 33 requests and 35 atomic questions, with resolved model
`jev-1.13.0`. A manual second evaluation of the identical native evidence and
unchanged corpus reproduced the results:

| Check | First run | Same-output repeat |
| --- | --- | --- |
| Exact/native outcome checks | 11/11 | 11/11 |
| Native candidates passing Jev | 9/11 | 9/11 |
| Controls matching engineering labels | 21/22 | 21/22 |
| Control false passes | 0 | 0 |
| Control false failures | 1 | 1 |
| Combined pass | No | No |

The native output retained the disposable opening `Um,` in the filled-pause
example and reduced the intentional `very very` emphasis to `very`. Both
findings were confirmed by reading the synthetic source/output, independent of
Jev's probabilities. They are follow-up cleanup issues, not changes made here.

Jev marked the expected-pass overlapping-disagreement control as failing, even
though `me: No, I I need another day.` retains the local speaker's disagreement
and request for another day. This is a mismatch with the engineering label.
The default suite returns exit 1 and retains it for review; it does not hide the
control, revise the threshold or claim a calibrated judge. The 0.10 margin and
questions remain unchanged between these two runs. Repetition on identical
outputs measures judge repeatability, not Apple model stability or general
accuracy.

Each live evaluation reported 15,199 input tokens. At the dated input estimate,
the two runs total $0.001276716; this is not a provider billing receipt. No
automatic retries, fallback providers, purchases or top-ups occurred.

## Local evidence

The complete `scripts/ci/local-gate.sh` passed on the testing checkout based on
`6d5477819bbb`. That includes full `validate.sh`, 345 Swift tests with two
expected skips and zero failures, ThreadSanitizer, AddressSanitizer, arm64
builds, local app/entitlement checks, package layout, checksums and disposable
update-signature checks. The two skips are the explicit native quality probe
and the external media fixture. The final source-contract gate also passed all
12 offline adapter tests, including credential isolation. Documentation checks
passed. Local package checks do not publish or install a release.

Gate logs are retained in `.build/jev/validation/`. Existing test-source compiler
warnings about two unmutated weak variables were observed; this change does not
alter those tests.

The ignored `.build/jev/` directory retains:

- `2026-09-23T05-02-40-511Z-66a4bea0/`: successful native/request preview.
- `2026-09-23T05-03-33-601Z-dd9edabc/`: first live native/Jev run.
- `2026-09-23T05-05-26-473Z-repeat/`: explicit repeat of the same native output.

Each report includes the source and corpus hashes, raw probabilities, candidate
text, resolved Jev version, usage and timing. The two live corpus hashes match:
`fa26115502fc19d8c9739269748ab2e14636935dc65beacd60e07dceda7e7400`.
These build directories are local evidence, not files to commit or publish.

The [testing guide](jev.md) explains setup, synthetic provenance checks,
offline/preview modes and interpretation. This run does not establish capture,
ASR accuracy, other-language behavior, or release acceptance. No version, app
implementation, entitlement, runtime dependency or release workflow was changed.
